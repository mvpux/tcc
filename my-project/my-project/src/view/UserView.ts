// View: Handles how data is presented to the user
// This is a simple view for displaying user information
export class UserView {
  // Displays a message to the user (console for simplicity)
  showMessage(message: string): void {
    console.log(message);
  }
}
